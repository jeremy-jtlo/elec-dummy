import React, { Component } from 'react';

export default class Main extends Component {
    render() {
        /* Skeleton for a main content panel */
        return(
            <div className="mainPanel">
                <h1>This is the Main Panel</h1>
            </div>
        );
    }
}